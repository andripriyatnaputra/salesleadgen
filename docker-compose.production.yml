version: '3.8'

services:
  # Backend - API Server & Scraping Agents
  backend:
    image: ghcr.io/andripriyatnaputra/salesleadgen-backend:latest
    container_name: starcom-leadgen-backend
    restart: unless-stopped
    pull_policy: always
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "3001:3001"
    environment:
      NODE_ENV: production
      PORT: 3001
      DATABASE_URL: postgres://sales:${POSTGRES_PASSWORD:-sales123}@host.docker.internal:5433/salesdb?sslmode=disable
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY}
      PENGADAAN_EMAIL: ${PENGADAAN_EMAIL}
      PENGADAAN_PASSWORD: ${PENGADAAN_PASSWORD}
      PENGADAAN_TOKEN: ${PENGADAAN_TOKEN}
      PENGADAAN_COOKIE: ${PENGADAAN_COOKIE}
      CIVD_USERNAME: ${CIVD_USERNAME}
      CIVD_PASSWORD: ${CIVD_PASSWORD}
      HUNTER_API_KEY: ${HUNTER_API_KEY}
    networks:
      - starcom-network

  # Frontend - Next.js Dashboard
  web:
    image: ghcr.io/andripriyatnaputra/salesleadgen-web:latest
    container_name: starcom-leadgen-web
    restart: unless-stopped
    pull_policy: always
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      - backend
    environment:
      NODE_ENV: production
      DATABASE_URL: postgres://sales:${POSTGRES_PASSWORD:-sales123}@host.docker.internal:5433/salesdb?sslmode=disable
      BACKEND_URL: http://backend:3001
    ports:
      - "3100:3100"
    networks:
      - starcom-network

networks:
  starcom-network:
    driver: bridge
